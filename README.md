This is a mern stack folders 

git branch react
git branch
git checkout react               ->enter the react branch
git rm -r branchname             -> delete all other branches (example : i have to create react branch now i have already css,html,js branch are there in that tym ,, in the react branch i want only react related files not other branches files at that tym we use this command)

git add react_folders
git commit -m "react added"
git push origin react
